<Window x:Class="LordOfTheCards.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Controls="clr-namespace:LordOfTheCards.Controls"
        mc:Ignorable="d"
        Title="LordOfTheCards" Height="800" Width="800">
    <Grid>
        <StackPanel>
            <Button Style="{DynamicResource ButtonStyle1}" Opacity="1" Margin="0 0 0 10" Width="200" Height="100" Click="NewGameClick" BorderThickness="0">
                New Game
            </Button>

            <Button Style="{DynamicResource ButtonStyle1}" Opacity="1" Margin="0 0 0 10" Width="200" Height="100" Click="LevelDesignerClick" BorderThickness="0">
                New Game
            </Button>

        </StackPanel>
    </Grid>
</Window>
