<Window x:Class="LordOfTheCards.Windows.DesignerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LordOfTheCards.Windows"
        mc:Ignorable="d"
        Title="DesignerWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="{x:Type Button}" x:Key="ImageButtonStyle">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <ContentPresenter/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <StackPanel>
            <Button Style="{StaticResource ImageButtonStyle}" Click="ImageButton_Click" Opacity="1" Margin="0 0 0 10" Width="200" Height="100"> 
                <Image Source="{Binding MyImageSource}"/> 
            </Button>
        </StackPanel>
    </Grid>


</Window>
